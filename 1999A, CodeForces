#include <bits/stdc++.h>
using namespace std;

int main() {
    int t; cin >> t;
    while(t--) {
        string a; cin >> a;
        string s1 = a.substr(0, 1);
        string s2 = a.substr(1,1);
        cout << stoi(s1) + stoi(s2) << endl;
    }
    return 0;
}
